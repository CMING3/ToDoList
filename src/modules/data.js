export default function dataBase(){
    
    /*
    task[{
            title: string,
            desc: string,
            priority: string,
            date: date,
            complete: boolean,
        }]
    */
    let task = [];
    let project = [];

    getLocalStorage()
}

const getLocalStorage = () => {
    //task & project .push(localstorage) 
}

